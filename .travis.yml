language: cpp

matrix:
    - os: windows

before_script:
  - if [ $TRAVIS_OS_NAME = windows ]; then choco install llvm --version=8.0.1 --allow-downgrade; fi
  - if [ $TRAVIS_OS_NAME = windows ]; then (New-Object net.webclient).Downloadfile("https://github.com/vovkos/llvm-package-windows/releases/download/llvm-8.0.0/llvm-8.0.0-windows-x86-msvc15-msvcrt.7z", "llvm.7z"); fi

script:
  - mkdir build && cd build && cmake .. && cat "C:/Users/travis/build/rickBuczynski/coolang/build/CMakeFiles/CMakeOutput.log"
